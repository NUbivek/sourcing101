# STRUCTURE

- `startup_watch/`
  - `startup_watch.py` (main script + source scraping functions)
  - `config.yaml` (local/full)
  - `config.github.yaml` (CI-safe)
  - `requirements.txt`
  - `run_once.sh`
  - `output/`
- `.github/workflows/startup_watch.yml`
- `SCM Companies PItchbook Extract/` (legacy OCR extraction scripts and outputs)
- `.planning/` (GSD planning docs)
- `.opencode/` (local GSD command/runtime files)
